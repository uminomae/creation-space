// Bump this when UI/interaction changes, after confirming:
// - scroll fade targets still hide/show as intended
// - reports modal and CTA rendering still match existing style
export const DEV_VERSION = '2026.03.04';
