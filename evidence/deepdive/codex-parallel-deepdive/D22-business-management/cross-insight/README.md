# D22 cross-insight サマリー

- 実行日: 2026-03-10
- 手法: cross-insight（横断分析）
- 入力: evidence本体(11件) + run1 + insight2 + GPTレビュー + GPT突き合わせ + 5段階モデル定義
- 出力: output.md

## 要点

D22 を横断すると、5段階モデルに対する最大の収穫は `縁` と `束` の校正にある。  
`縁` は関係網・未決定性・渦への駆動が揃う界面として、`束` は継承可能な残存構造として定義した方が精度が高い。  
一方で `場` は薄く、D22 ではゼロ地点より既存構造を緩めた後の再到達点として現れやすい。
