# Phase 4 Prep Summary（Phase 3 GPT Review抽出）

## D14 心理学（`chatgpt/output/0228/D14pro_review_output_v2.md`）

1. **総合判定**
- 個別エントリの査読結果ではなく、`REQは変更せず evidenceのみレビュー` という方針・テンプレート提示文書。Phase 4突き合わせに必要な領域別判定情報は未収録。

2. **要修正（P0/P1）**
- `P0` エントリ単位レビュー（ID付き指摘）の未実施。`EV-...` 等のエントリIDは本文に記載なし。

3. **要議論**
- 当該ファイルをD14レビュー成果物として扱うか、別の実レビュー版（エントリID付き）を再取得するか。

4. **見落とし指摘**
- 記載なし（抽出不可）。

5. **信頼度への指摘**
- 記載なし（抽出不可）。

---

## D15 美学（`chatgpt/output/0302/d15deep-research-report.md`）

1. **総合判定**
- 理論選定の幅は広く有用だが、書誌誤り・集計不整合・`[P]/[M]`境界の曖昧さが残り、現状は「要修正」。
- とくにEV-AE-007とL-1/L-2の不整合が、Phase 4の突き合わせ精度を直接下げる。

2. **要修正（P0/P1）**
- `P0` `EV-AE-007`: 「倒理的」→「倫理的」修正、Rancière邦訳の訳者誤記修正（「梯久美子訳」→「梶田裕訳」）。
- `P0` `L-1`: 縁🔴件数の自己矛盾修正（最終値4件で確定）。
- `P0` `L-2`: 「6タイプ」表記と列挙数（実質8分類）の不一致修正、Eco行の型ラベルと説明語の不整合修正。
- `P1` `EV-AE-002`: `proposer` と `refs` の不整合修正、`[P]`主張と解釈（`[M]`）の分離。
- `P1` `EV-AE-001`: Klein/Bion参照不足の補完、因果断定表現の緩和。

3. **要議論**
- `EV-AE-002` の縁フラグ（🟡維持か🔴化か）の妥当性。
- `EV-AE-003/004/007` のAccept根拠（縁の3条件）をどこまで明文化するか。

4. **見落とし指摘**
- 追加候補として `Baumgarten` / `Hume` / `Gadamer` が提示されている。

5. **信頼度への指摘**
- 全件「中」はフラットすぎるため差分付けが必要。
- `EV-AE-005`（Dewey）・`EV-AE-008`（Schiller）は上方（高寄り）候補。
- `EV-AE-006`（Benjamin）・`EV-AE-007`（Rancière）は下方（中下〜低）候補。

---

## D16 歴史学（`chatgpt/output/0302/d16deep-research-report.md`）

1. **総合判定**
- 理論辞書としての有用性は高いが、「複数時間層」と「5段階連鎖」の写像規則未定義が主要ボトルネックで、現状は「中〜要修正」。
- 循環理論の`束→場回帰`定義揺れと総括集計不整合が、突き合わせ時の判定を不安定化させる。

2. **要修正（P0/P1）**
- `P0` `L-2`: 「8タイプ」表記と実際の列挙（10項目）の不整合修正。
- `P0` `EV-HI-001`: ブローデル三層を段階連鎖へ直写する記述を修正（`層×段階`の二次元化）。
- `P1` `EV-HI-003` / `EV-HI-004` / `EV-HI-009` / `EV-HI-010`: `束→場回帰`の意味を統一定義（完全交替か残存螺旋か）。
- `P1` 全体: ID表記揺れ（`EV-HI-xxx` / `HI-xxx`）の統一。

3. **要議論**
- 循環理論の回帰を `回帰A（完全交替）` と `回帰B（残存・螺旋）` に分ける運用可否。
- `EV-HI-005`（世界システム）の縁フラグを🟡維持か🔴上方修正か。

4. **見落とし指摘**
- 主要候補として `Marx` / `Spengler` / `Collingwood`。
- 補助候補として `Fukuyama` / `Hobsbawm` / `Scott` / `Le Roy Ladurie`。

5. **信頼度への指摘**
- エントリ全体一律ではなく「主張単位」での確度付けを要求。
- `EV-HI-001` の写像部分や `L-2` 集計は低寄り、`EV-HI-004` `EV-HI-006` `EV-HI-010` の一次裏付け主張は高寄り。

---

## D17 言語学（`chatgpt/output/0302/d17deep-research-report.md`）

1. **総合判定**
- 全体構造は良好だが、書誌誤りと集計不整合、`[M]`側の同一視表現が残るため「要修正」。
- とくにEV-LI-008の書誌誤りはP層の即時修正対象。

2. **要修正（P0/P1）**
- `P0` `EV-LI-008`: Boroditsky (2001) 書誌誤り修正（`Cognition`表記を修正）。
- `P0` `L-2`: 「9タイプ」表記と実際の10分類の不整合修正。
- `P1` `EV-LI-010`: 「ボトルネック=欠損」の同一視を修正し、「情報制約」表現へ。
- `P1` `EV-LI-008`: claimで触れる `Winawer et al. (2007)` 等の根拠文献追記。
- `P1` `EV-LI-009`: 「制約は2種類」の断定緩和（「大別すると」）と出版経緯注記追加。
- `P1` `EV-LI-002` / `EV-LI-003`: 「決定する」断定語の緩和。

3. **要議論**
- `EV-LI-003` / `EV-LI-008`（CA）のAccept引き上げ条件の明文化。
- `EV-LI-009`（OT）における「縁」定義（形式競合を縁とみなすか）。

4. **見落とし指摘**
- 追加候補として `Weinreich–Labov–Herzog (1968)`。
- 補強軸として「伝達（子ども）と拡散（成人間）の区別」「参照枠研究（空間認知）」が提示。

5. **信頼度への指摘**
- 全件中は運用上許容だが、`[P]信頼度` と `総合信頼度（[M]含む）` の分離が必要。
- `EV-LI-005`（NSL）・`EV-LI-010`（ILM）は`[P]`層のみなら高寄り。

---

## D18 社会学（`chatgpt/output/0302/D18_sociology_review.md`）

1. **総合判定**
- 構造は良好で重大破綻はないが、理論精度補強と信頼度設計の分離が必要で「要修正」。
- 最大修正点はSO-005（三契機の扱い）。

2. **要修正（P0/P1）**
- `P0` `SO-005`: 「三契機は時間順ではない」の断定を修正（弁証法的モーメントとしての順序性と循環性を明示）。
- `P1` `SO-001`: Luhmannの用語補強（operationally closed / cognitively open / structural coupling）。
- `P1` `SO-010`: interaction ritualの4要件に「外部境界」を追記。
- `P1` `L-2`: 「縁9タイプ」の数え方・算定基準明示。

3. **要議論**
- `SO-001` のCA維持は妥当だが、検証可能な具体事例で補うか。
- `SO-005` の3契機と5段階写像をphase概念としてどう定義するか。
- `SO-008` の縁⚪維持の妥当性。

4. **見落とし指摘**
- 追加候補として `Weber`（優先高）、`Coleman`（優先中）。

5. **信頼度への指摘**
- `Granovetter`高、`Luhmann`（写像）低〜中、`Collins`中〜高という勾配提案。
- 内容信頼度と写像信頼度の分離が必要。

---

## D19 文学（`chatgpt/output/0302/d19deep-research-report.md`）

1. **総合判定**
- 理論カバレッジは広いが、原典照合未完了箇所が中核論点（特にTodorov）に残り、全件Acceptは過大で「要修正/要議論」。
- `[P]`（原典準拠）と`[M]`（対応づけ）の分離不足が、Phase 4突き合わせの主要リスク。

2. **要修正（P0/P1）**
- `P0` `EV-LT-005`: Todorov「5要素」一次照合未完了のまま断定している点を修正（照合完了までCA扱い推奨）。
- `P1` `EV-LT-010`: Bloom 6修正比の訳語・作用定義を原典準拠で再統一、`6→5`写像は未解決課題として明示。
- `P1` `EV-LT-001`: Propp `31→5`圧縮を「ヒューリスティック（粗視化）」として明示し、情報損失と射程条件を注記。
- `P1` `EV-LT-009`: focalization と voice の区別を明確化し、抱持接続を限定的に記述。

3. **要議論**
- 全10件Accept運用の妥当性（未確認・未済注記との整合）。
- `EV-LT-005` をL-1/L-4の中核根拠に据える運用継続可否。
- `EV-LT-009`（Genette）縁⚪の妥当性再評価。

4. **見落とし指摘**
- 本レビュー文書内での「追加理論家候補」明示は限定的（主軸は既存10件の原典照合強化）。

5. **信頼度への指摘**
- 「全件Accept・信頼度中」は過大評価リスク。
- 一次照合完了まで `EV-LT-005` など中核項目はCA/低寄りを含む再配分が必要。

---

## D20 法学・政治学（`chatgpt/output/0302/d20deep-research-report.md`）

1. **総合判定**
- 編集方針（制度/規範生成を縁設計で読む）は有効だが、本文欠損と典拠の当て方の問題で「要修正」。
- とくにP層検証可能性（全文表記・参照粒度）が不足しており、Phase 4での突合時に詰まる。

2. **要修正（P0/P1）**
- `P0` `LP-007`: 「無知のヴェ...」欠損解消（原初状態定義の全文復元）。
- `P0` `LP-009`: 3流れ名称・説明の欠損解消（problem/policy/politicsの明示）。
- `P0` `LP-005`: UNCITRAL根拠と「交渉→調停→仲裁→訴訟」一般モデルの混同を分離。
- `P0` 複数エントリ（`LP-001/003/008`等）: `...`欠損除去と`[P]`主張への参照紐付け統一。
- `P1` `LP-008` / `LP-009`: 5段階写像の限界注記（理想型である旨）を補強。
- `P1` `L-2` と `L-4#1`: Rawlsの「縁」実装（反照的均衡 vs ヴェール）の階層関係を定義。

3. **要議論**
- 全10件Accept運用を維持するか（欠損・参照不足が残る状態での扱い）。
- `L-2`の10タイプ分類基準（ラベル定義）をどこまで形式化するか。

4. **見落とし指摘**
- 補強候補として、正当性類型・支配正当化類型・input/output legitimacy系の理論枠組みが示唆されている。

5. **信頼度への指摘**
- 全件「中」は粗く、`P信頼度` と `M信頼度` の二軸化が必要。
- 修正後は `LP-003`（Ostrom）や `LP-009`（Kingdon）のP層を高寄りに再評価可能。

