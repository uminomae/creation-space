interface:
  display_name: "Creation Reports Workflow (Draft)"
  short_description: "Draft checks and PDF flow for Creation reports"
  default_prompt: "[DRAFT] Run the creation reports workflow: scan impact, update paths, generate EN PDFs, validate links, and leave notes for next-session refinement."
